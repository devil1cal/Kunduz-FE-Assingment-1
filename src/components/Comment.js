// not working

//import
///import React from "react";

//style

//components
//import useCommentFetcher from "../hooks/commentFetcher";

//node

/*const ShowComments = ({ data, op, type }) => {
 *  const { comments } = useCommentFetcher(type ? type : "kids");
 *
 *  return (
 *    <React.Fragment>
 *      <React.Fragment>
 *        {comments.map((comment) => (
 *          <Comment key={comment.id} data={comment} op={op} />
 *        ))}
 *      </React.Fragment>
 *    </React.Fragment>
 *  );
 *};
 *
 *export default ShowComments;
 */
