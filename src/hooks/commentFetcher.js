// not working

//import
//import { useState, useEffect } from "react";
//import { getPostDataWithComments } from "../utils/apis";

//style

//components

//node

/*const useCommentFecther = (type) => {
 * const [comments, setComments] = useState([]);
 * const [isLoading, setIsLoading] = useState(false);
 *
 * useEffect(() => {
 *   setIsLoading(true);
 *   getPostDataWithComments(type)
 *     .then((comments) => {
 *       setComments(comments);
 *       setIsLoading(false);
 *     })
 *     .catch(() => {
 *       setIsLoading(false);
 *     });
 * }, [type]);
 *
 * return { isLoading, comments };
 *};
export default useCommentFecther; */
